# API Status (v0.44.0)

Full namespace-by-namespace parity status with Windows BG3SE.

Overall parity remains approximately 94.8% pending recomputation after Wave 7 live verification. The current figure comes from the [ROADMAP.md matrix](../ROADMAP.md#feature-parity-matrix) under behavioral accounting with per-function contract diffs against the Windows registration blocks (admitted fail-closed stubs score zero; macOS-only extras earn no credit; matched-upstream contracts leave the denominator). Every intentionally fail-closed API is cataloged in [docs/deferrals.md](../docs/deferrals.md).

- **Osi.*** - Dynamic metatable (40+ functions), **OsirisFunctionHandle encoding** (v0.36.39), crash-resilient dispatch with breadcrumbs
- **Ext.Osiris** - RegisterListener, NewCall/NewQuery/NewEvent (server context guards)
- **Context System** - Ext.IsServer/IsClient/GetContext, two-phase bootstrap (v0.36.4)
- **Ext.Entity** - GUID lookup, **1,999 components registered** (462 layouts: 169 verified + 293 generated), **1,730 sizes** (1,577 Ghidra + 153 Windows-only, 87% coverage), GetByHandle, **Dual EntityWorld Complete** (client + server auto-captured), **Entity Events** (Subscribe/OnCreate/OnDestroy + 8 variants, Unsubscribe — salted pool, deferred queue, per-entity hooks), **CreateComponent** (verified ComponentOps registry at EntityWorld+0x390, vptr slot 5), and archetype enumeration. **19/26 (73.1%)** of the Windows Entity.inl registration block is behavioral. Wave 7 A1 added **HandleToUuid, UuidToHandle, GetAllEntitiesWithUuid, GetRegisteredComponentTypes** (`mapped`/`oneFrame` filters), and **GetEntitiesAroundPosition** (Windows 2D XZ-circle behavior). Remaining gaps: Create, Destroy, OnSystemUpdate, OnSystemPostUpdate, GetTrace, ClearTrace, and the EnableTracing stub. B6 made the two system-update callbacks implementation-ready, but they remain gaps until the wrappers land.[^entity-stubs]
- **Ext.Stats** - **100% Windows API function-count parity** (52 functions), **50/52 behavioral (96.2%)**: Get/GetAll/Create/Sync, CopyFrom, SetRawAttribute, AddEnumerationValue, TreasureTable/TreasureCategory, and all StatsObject methods. `Get(name, level)` matches Windows by ignoring the DOS2-vestige `level` argument; Stats.inl:479-508 performs name-only lookup.[^stats-stubs]
- **Ext.Events** - 33 events with priority ordering, Once flag, Prevent pattern (13 lifecycle + 17 engine + 2 functor + 1 network events), **runtime mod attribution** (per-handler mod tracking, soft-disable, health stats)
- **Ext.Timer** - **20 functions**: WaitFor, WaitForRealtime, Cancel/Pause/Resume, GameTime/DeltaTime/Ticks, **Persistent timers** (save/load support)
- **Ext.Vars** - PersistentVars, User Variables, Mod Variables
- **Ext.StaticData** - Immutable game data (**10 types**: Feat, Race, Background, Origin, God, Class, Progression, ActionResource, FeatDescription, CharacterCreationAppearanceVisual via ForceCapture; typed field layouts in `src/staticdata/staticdata_layouts.c`, canonical GUID text)
- **Ext.Resource** - Non-GUID resources (34 types: Visual, Material, Texture, Dialog, etc.)
- **Ext.Template** - Game object templates (14 functions, 10 properties, type detection via VMT)
- **Ext.Types** - **10/13 (76.9%)** against the 14-function Windows registration block. Wave 7 A3 makes Construct's error surface exact (`Unknown type name`, `Unable to construct non-object type`, `Type ... is not constructible`); valid object types reach the shared upstream TODO and return 0 values, so Construct remains matched upstream and leaves the denominator. Behavioral: GetAllTypes (~2050), GetTypeInfo, GetObjectType, TypeOf, IsA, Validate, **GetValueType**, **GetFunctionLocation**, and **Serialize/Unserialize**. Missing: GetHashSetValueAt and **AddCustomFunction/AddCustomProperty**. GetComponentLayout, GetAllLayouts, and GenerateIdeHelpers are macOS extras outside the denominator.
- **Ext.Debug** - Memory introspection (ReadPtr, ProbeStruct, HexDump), **mod diagnostics** (ModHealthCount, ModHealthAll, ModDisable), **observability APIs** (GetHookStatus, GetVersionStatus, GetCacheStats, GetEventStatus, GetManagerStatus)
- **Ext.IMGUI** - **Complete widget system** (40 widget types): NewWindow, AddText, AddButton, AddCheckbox, AddInputText, AddCombo, AddSlider, AddColorEdit, AddProgressBar, AddTree, AddTable, AddTabBar, AddMenu, handle-based objects, event callbacks (OnClick, OnChange, OnClose, OnExpand, OnCollapse)
- **Ext.Mod** - Mod information (5 functions): IsModLoaded, GetLoadOrder, GetMod, GetBaseMod, GetModManager
- **Ext.Level** - **20/25 (80%)**: TestBox, TestSphere, **GetHeightsAt** (real multi-subgrid walk matching Ai.inl:262/406 — Wave 7, replacing the stub the B3 truth pass exposed), GetCurrentLevel, GetPhysicsScene, GetAiGrid, IsReady, all 8 sweeps (incl. **SweepCylinderClosest/All** at proven VMT 14/18), **GetEntitiesOnTile**, and the pathfinding suite (**GetPathById, ReleasePath, GetActivePathfindingRequests, FindPath** — verified AiGrid/AiPath/PathMap layouts, `-1337` sentinel rejection, copied-state-only returns); physics dispatch repaired against the audited macOS vtable (9 wrong-by-1 indices fixed, `ghidra/offsets/PHYSICS_VMT_AUDIT.md`); **GetTileRawDebugInfo** diagnostic surface (raw flags/masks + confirmed MinHeight +0x0a, no parity credit); 5 deferrals: RaycastClosest/All (quarantined by-value C++ params), **RaycastAny** (Wave 7 B4b: real VMT-slot-10 binding over the proven zeroed-optional ABI, arm64+UUID+version gated — still a scored deferral, no parity credit until the live stress ladder passes), GetTileDebugInfo, BeginPathfinding (docs/deferrals.md)
- **Ext.Audio** - **16/16 Windows-registered (100%)** (GetSoundObjectId and IsReady are macOS extras outside the contract denominator): PostEvent, Stop, PauseAllSounds, ResumeAllSounds, SetSwitch, SetState, SetRTPC, GetRTPC, ResetRTPC, LoadEvent, UnloadEvent, GetSoundObjectId, IsReady, PlayExternalSound (STDString ABI), and **LoadBank/UnloadBank/PrepareBank/UnprepareBank** (dlsym'd AK::SoundEngine exports)
- **Ext.Net** - **7/7 Windows registrations behavioral**: PostMessageToServer, PostMessageToUser, PostMessageToClient, BroadcastMessage, Version, IsHost, and **PlayerHasExtender(guid)**. Wave 7 A6 resolves GUIDs through the peer manager; unknown characters and unassigned users return nil, otherwise it returns the peer extender capability. IsReady, PeerVersion, request/reply callbacks, and the RakNet backend extend the macOS surface.
- **Ext.RegisterNetListener** - Per-channel network message listener (MCM backbone)
- **Net.CreateChannel** - High-level NetChannel API for multiplayer mod sync (SetHandler, **SetRequestHandler**, SendToServer, **RequestToServer with callbacks**, SendToClient, Broadcast)
- **Ext.Utils** - Compatibility aliases (6 functions): Print, PrintWarning, PrintError, Version, MonotonicTime, GetGameState
- **Ext.Math** - **59/59 (100%)**: vector, matrix, quaternion, and scalar utilities, including Random, Fract, **Smoothstep**, and **IsNaN**
- **Ext.Localization** - Localization API (4 functions): GetLanguage, CreateHandle, **GetTranslatedString**, **UpdateTranslatedString** (live-verified round trip; update verified against the returned pool handle)
- **Ext.ModEvents** - Per-mod cross-mod event system: Subscribe, Throw, Unsubscribe (MCM compat)
- **Osi.DB_*** - Generic database accessor with **Get + Delete**. Wave 7 A5 Delete performs exact-match row deletion with RETE propagation; exact-arity errors match Windows, wildcard deletion is unsupported on both platforms, and a missing row is a no-op.
- **Entity** - Extended entity API: **CreateComponent**, **RemoveComponent**, **GetEntityType**, **GetSalt**, **GetIndex**, **GetNetId**
- **Events** - **ExecuteFunctor** Dobby hook, BeforeDealDamage/DealDamage fire live (build 7209685, verified 51/51 paired)
- **Version Detection** - Sentinel address probes for game version mismatch tolerance (Issue #78)

[^stats-stubs]: Remaining gaps behind function-count parity: AddAttribute returns false and ExecuteFunctors is partial. AddEnumerationValue is implemented through engine `ValueList::Insert` with FixedString interning (`ghidra/offsets/VALUELIST_INSERT.md`), but the 2026-08-04 live session found the enum registry resolving NULL for every name — read accessors included — a live regression under investigation (docs/parity-100/LIVE-VERIFICATION-2026-08-04.md). Passive sync remains false after a PARTIAL-GO recon: scalar/description and three functor lists are safe, but `Boosts` is blocked on an LTO closure ABI (`ghidra/offsets/PASSIVES_PARSE_RECON.md`). Interrupt sync remains false. TreasureTable/TreasureCategory reads, GetStatsLoadedMods, and spell/status prototype sync return real data.
[^entity-stubs]: EnableTracing and DisableTracing are warn-and-nil stubs (`src/injector/main.c`); `entity:Replicate()` is a no-op. `entity:GetReplicationFlags(component[,qword])` is now a real **read-only** proxy method matching the Windows entity-proxy placement (SyncBuffers chain, `ghidra/offsets/REPLICATION_SYNCBUFFERS.md`) — fail-closed, version-gated, and earns no parity credit until the live-probe checklist validates the runtime chain (W7-C step 2 of 9). GetAllEntities, GetAllEntitiesWithComponent, GetAllEntitiesWithUuid, GetAllComponents, and GetEntitiesAroundPosition are real archetype walks. The spatial query matches the Windows XZ-circle contract through CharacterComponent/item-marker walks plus TransformComponent because the ARM64 GridStructure layout remains unrecovered. `entity:CreateComponent` dispatches through the verified ComponentOps registry; `entity:RemoveComponent` returns false. Component property writes are real for INT32, UINT8, BOOL, FLOAT, and INT32_ARRAY. FLOAT_ARRAY writer code is present with exact-length and finite-value checks plus staged atomic commit, but A7 earns no parity credit until `ls::EffectComponent::OverrideFadeCapacity` ARM64 offsets are verified.
