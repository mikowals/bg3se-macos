You reported that delete, restart and reinstall restored normal operation, and no BG3 build, extender commit, session log or crash report was attached, so there is nothing reproducible left to chase. Closing as resolved locally. If it recurs, open a new issue with `latest.log`, the newest BG3 `.ips` report from `~/Library/Logs/DiagnosticReports`, the BG3 build and the exact extender commit.
